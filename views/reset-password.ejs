<!doctype html>
<html lang="<%= lang %>">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= t('reset_password') %> &mdash; <%= t('app_title') %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>

<%- include('_topbar') %>

<main class="container py-5 app-shell mx-auto">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-5">

      <div style="text-align:center;margin-bottom:1.5rem">
        <span style="font-size:2.5rem;color:var(--gold)">&#10022;</span>
        <h1 style="font-family:'Playfair Display',Georgia,serif;font-size:2rem;margin:.25rem 0 0"><%= t('reset_password') %></h1>
      </div>

      <div class="page-card">
        <div class="card-body p-4">
          <% if (error.length) { %><div class="alert alert-danger"><%= error[0] %></div><% } %>

          <form method="post" action="/reset-password" class="d-grid gap-3">
            <input type="hidden" name="token" value="<%= token %>">
            <div>
              <label class="form-label"><%= t('new_password') %></label>
              <input class="form-control" type="password" name="password" autocomplete="new-password" minlength="8" required>
              <div class="form-text"><%= t('password_min_length') %></div>
            </div>
            <div>
              <label class="form-label"><%= t('confirm_new_password') %></label>
              <input class="form-control" type="password" name="confirm_password" autocomplete="new-password" minlength="8" required>
            </div>
            <button class="btn btn-primary w-100" type="submit"><%= t('reset_password') %></button>
          </form>
        </div>
      </div>

    </div>
  </div>
</main>
</body>
</html>
